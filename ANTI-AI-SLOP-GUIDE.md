# QDU Wasteland · 去 AI 味设计规范（Anti-AI-Slop Studio Guide）

> 目的：让这个网站的视觉尽量"有人味、有编辑感、不像 AI 生成"。本规范把行业成熟的 anti-AI-slop 方法论（自 `open-design-main/skills/taste-skill`、`impeccable-design-polish`、`design-review`、`web-design-guidelines` 提炼，并针对本项目"Go 后端 + 原生 HTML/CSS/JS、校园课程社区"的实际情况做了中文化、裁剪与辨证）转译成我们项目可执行的规则。
> 相关：`VISUAL-REVIEW.md`（美学评审：一致性问题）· `VISUAL-SYSTEM-OVERVIEW.md`（视觉范式）· `product-plan.md`（主方案）
> 状态：规范稿，供你（站长）定夺哪些采纳。

---

## 〇、先想清楚：什么是"AI 味"？

AI 生成的界面通常有几种"一眼假"的共性：
1. **颜色堆砌**：紫蓝渐变、霓虹外发光、满屏饱和度。
2. **千篇一律的结构**：三个等宽特性卡片、居中的 hero、每节都放一个"小标签眉"（eyebrow）。
3. **排版泛滥**：到处 emoji、滥用省略号/破折号/中间点当装饰、超大 H1 硬吼。
4. **假细节**：版本号 v0.6、假评分进度条、假库存计数器、"Scroll ↓"滚动提示。
5. **文案用力过猛**：「颠覆」「赋能」「无缝」「丝滑」这类空洞动词。

我们项目已有了好底子（品红 + 纸感 + 克制），规范的目标是**把剩下的 AI 味清掉，让它像人认真排出来的编辑品**。

> ⚠️ 辨证前提：本规范源自英文设计界的 anti-AI-slop。**中文排版习惯不同**，如中文破折号"——"、顿号"、"、间隔号"·"是正常中文标点，不能机械按英文规则一刀切。规范里凡涉及标点，我们都按"中文编辑规范 + 克制"处理，而不是照搬英文禁令。

---

## 一、颜色：落到一个统一的调色板，消灭"堆砌"

**🔴 硬规则**
1. **每页至多一个强调色**。我们全站主色定为品红 `--magenta`，全站锁定；不要在某个区块突然跳成蓝/紫/绿去'点缀'（当前 `search` 徽标的蓝 #3b82f6、紫 #8b5cf6 属于此类的反面案例 → 收敛进语义色板或改用品红系）。
2. **不用纯黑 #000000 与纯白 #ffffff**。深色用近黑（`--ink:#1a1a2e` 已对），浅面用米白（`--paper`）。
3. **不要霓虹外发光**（`box-shadow` 大面积彩色辉光）。阴影一律"染成背景色调"的柔和投影，不用纯黑 drop shadow。
4. **避免给大标题做花哨渐变文字**。我们的 hero 标题已是品红→橙渐变——可保留（这是刻意的品牌效果），但**不要在正文/卡片标题重复这类渐变**。
5. **情绪色补齐语义变量**：`--error / --success / --warn` 必须存在并全站统一，别再到处写 `#d32f2f`。

**🟡 建议**
- 建立 `:root` 语义色板，所有硬编码色替换为变量（当前 me.css 26 个、courses.css 22 个硬编码色 → 收敛）。
- 图标/徽标/状态色都要进这个板子，不许临时造色。

---

## 二、排版：用「字重+字号层级」说话，不用"吼"

**🔴 硬规则**
1. **控制 H1 尺寸**：不是越大越高级。用字重、字距、颜色拉开层级即可。超大显示字（9rem）只留给首页 hero 一个主角，内页不要每个标题都巨字。
2. **同一处强调只用同字体的斜体/加粗**，不要为了"有设计感"在无衬线标题里硬塞一个衬线词（混排家族=业余）。
3. **中间点"·"要克制**：作为元信息分隔，**每行最多 1 个**。当前很多文案"比赛 · 老师 · 生活方式 · 社团"连排，宜改成"换行 / 顿号 / 分区"的某种更干净的形式，不要通篇"foo · bar · baz"。

**🟡 建议**
- **用户界面字体与阅读冲突**：正文建议保留无衬线（Aptos/雅黑）保证可读；"关于学校"深度文章模块才用衬线（宋体系）制造编辑感——这和我们既定分工一致。

---

## 三、结构与布局：打破"三卡平铺"套路

**🔴 硬规则**
1. **禁止"三张等宽特性卡片"** 作为通用版式。（当前首页 `module-grid` 是 4 张等宽模块卡——可考虑改成 2+2 或不对称，避免又一个模板脸。）
2. **hero 顶部留白有上限**：`padding-top` 最多约 6rem，超过就像布局 bug 而不是呼吸感。
3. **hero 内容最多 4 个文字元素**：眉/标题/副文/CTA。不要在 hero 里塞"受信任图标墙""价格提示""特性列表"——这些放 hero 以下。
4. **每个区块避免样式重复**：一张页面里，同一套布局族（如"左图右文"）最多出现 2 次，第 3 次必须换布局。
5. **不用等分数值制造假精确**：`92%`、`4.1×`、`48k` 这类数据要么来自真实数据，要么标注是示例。别编"工程感精确"数字。

**🟡 建议**
- 卡片没有必要就不加投影；用 `border-t`/`divide-y`/留白分组，而非"每样东西都浮起来"。
- 长列表（>5 项）换 UI 组件（分栏、网格、折叠），别用"每行一条下划线"的单调长表。

---

## 四、动效：只放有理由的，且克制

**🔴 硬规则**
1. **动效必须有理由**：说明层级、讲故事、给反馈、表示状态变化。理由只有"好看"就砍掉。
2. **每个页面至多 1 个跑马灯（marquee）**（首页 hero 已用，其他页面不再加）。
3. **`prefers-reduced-motion` 必须支持**：用户开"减弱动态效果"时，所有动画退化为静态。
4. **只动画 `transform` 和 `opacity`**，不动画 `top/left/width/height`（性能）。
5. **hover 物理反馈统一**：`transform`/`scale` 微移 + 阴影即可，全站一致，别一半卡片 translateX、一半 translateY。

**🟡 建议**
- `backdrop-filter: blur` 谨慎：低性能设备卡顿；要模糊就够明显（不用 2px 这种看不出又耗性能的）。

---

## 五、文案：把"AI 腔"翻译成人话（重点）

**🔴 硬规则**
1. **禁空动词**：无缝、赋能、颠覆、丝滑、极致、引领、打造生态……见到就换成具体动词。
2. **Every string 过一遍**：提交前读所有可见文案，删掉"听起来聪明但空洞"的表述（被动式谦虚、伪工匠标签、假装诗意的 meta 句）。
3. **版本号/假细节不上页面**：`v1.4.2`、`build 0048`、`last sync 4s ago`、假库存计数——一律不出现。

**🟡 辨证处理（中文标点）**
- **中文破折号"——"**：中文是合法标点，但**一篇页面里最多 1-2 处**作强调用，不要句句都是。当前 footer"——青岛大学学习互助社区"、hero 副题"——属于青岛大学自己的数字荒原"建议**删掉破折号**，改成直接陈述（"青岛大学自己的数字荒原。"），更干脆。
- **间隔号"·"**：中文里作并列分隔合法，但克制使用，别成堆。（43 处 → 收敛到每处元信息一个）
- 顿号"、"是中文排并列的正主，正文多用顿号，少用"·"。

---

## 六、图标与 Emoji：一刀切去 Emoji

**🔴 硬规则**
1. **Emoji 不做 UI 元素**：删除标题里的 📂💬🗓🏆🌱🎪 等（`.course-detail-h2`、`course.js` 的 📚💬、`articles.html` 的分类 emoji）。模块图标改用**一套统一线宽（strokeWidth 1.5/2）的 SVG 图标库**，全站一个风格。
2. **不用手画 SVG 图标**：需要就引图标库（本项目原生无构建，可内联少量精选 SVG 或引入 Tabler/Phosphor 的 SVG 文件），全站同一族。
3. **头像不用灰色占位 SVG"蛋"**：用真实的用户首字/首字母或风格化占位，别用图库默认人形图标冒充头像。

---

## 七、空态 / 加载 / 错误：把"状态"也设计上

**🔴 硬规则**
1. **加载**：用与最终布局同形的骨架屏（而非通用转圈）。
2. **空态**：要美观，并告诉用户怎么填。
3. **错误**：清晰、就地显示（表单下方），toast 只用于瞬时。
4. **按钮对比度**：任何 CTA 文字对背景对比度 ≥ WCAG AA（正文 4.5:1）。不允许"白底白字""透明钮无边框"。

**🟡 建议**
- `.stat-card.active` / `.my-tab.active` 等选中态必须有**可见的** CSS 视觉（当前有的缺失，见评审）。
- 表单 label 放输入框上方，错误放输入框下方，不要用 placeholder 冒充 label。

---

## 八、去 AI 味的"快速自检清单"（改完每个页面跑一遍）

- [ ] 每页只有 1 个强调色？有没有突然跳出的蓝/紫/绿？
- [ ] 有没有用纯黑/纯白？（应是近黑/米白）
- [ ] 有没有霓虹外发光 / 大面积渐变文字？
- [ ] hero 是不是"满信息堆叠"？（应 ≤4 文字元素）
- [ ] 有没有三张等宽卡的老模板？（应打破）
- [ ] 有没有"第 3 个"重复布局族？
- [ ] emoji 是否已全部替换成 SVG 图标？
- [ ] 可见文案里，破折号/中间点是否克制？有没有"无缝/赋能/极致"？
- [ ] 有没有假数字 / 版本号 / 假滚动提示 / 假评分条？
- [ ] 空态/加载/错误/选中态 是否都有设计？
- [ ] `prefers-reduced-motion` 是否生效？
- [ ] 圆角、按钮、字体是否全站一致（一档令牌）？

---

## 九、落地优先级（供你定夺）

| 优先级 | 动作 | 对应规则 |
|--------|------|---------|
| **P0** | 删 emoji → 换统一 SVG 图标；收敛徽标色到语义色板 | 六 / 一 |
| **P0** | 补语义色变量（error/success/warn），硬编码色全部替换 | 一 |
| **P0** | 圆角 / 按钮 / 字体 三档令牌统一（common.css） | 评审S1-S3 |
| **P1** | hero/页脚文案去破折号、"。"直接陈述 | 五 |
| **P1** | 首页 module-grid 打破"4 等宽卡"模板 | 三 |
| **P1** | 空态/加载/错误/选中态补齐视觉 | 七 |
| **P2** | 逐页面跑"去 AI 味自检清单" | 八 |

---

## 十、方法来源

- `open-design-main/skills/taste-skill/SKILL.md` —— anti-slop 前端方法论（dial 三件套、AI tells、em-dash 禁令等，本项目按中文语境辨证采纳）
- `open-design-main/skills/impeccable-design-polish`、`design-review`、`web-design-guidelines` —— 打磨/评审/规范
- `shucourse-recon2/VISUAL-SYSTEM-OVERVIEW.md` + `design-dna.json` —— 我们自己的视觉范式
- `qdu-wasteland/VISUAL-REVIEW.md` —— 现状问题评审
